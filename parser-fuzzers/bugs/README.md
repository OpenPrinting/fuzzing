# Optional Local Bug Metadata

This directory is intentionally empty in the public tree.

Private reproducers, issue reports, minimized crashing inputs, and local
triage notes should not be committed here. If a local evaluation needs
ground-truth metadata, create private `bugs/<id>/meta.yaml` files outside the
public review flow and keep `known_poc_allowed_in_seed: false`.
