# Public Seed Policy

This directory is for weak public seeds only.

Private reproducers, minimized crash inputs, and local issue artifacts must not
be copied here. Local metadata, when used, should keep
`known_poc_allowed_in_seed: false`.
