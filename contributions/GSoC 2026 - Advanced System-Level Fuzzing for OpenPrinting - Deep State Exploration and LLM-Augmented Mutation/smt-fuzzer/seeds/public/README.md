# Public Seed Policy

This directory is for weak, generic public seeds only. Discovery campaigns build
structured PPD and document inputs from templates at runtime instead of relying
on private reproduction artifacts.
